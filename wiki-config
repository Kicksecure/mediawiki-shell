#!/bin/bash

## TODO:
## create a file:
## credentials
## with the following contents:
#[[ -v USERNAME ]] || USERNAME="username"
#[[ -v USERPASS ]] || USERPASS="password"
[[ -v WIKI_SCRIPT_FOLDER ]] || WIKI_SCRIPT_FOLDER="w"
